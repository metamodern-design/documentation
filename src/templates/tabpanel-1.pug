include /_mixin-card.pug
include /_mixin-tabpanel.pug


+tabpanel('1', routes[1].label)
  section.px-1b
    h2.pb-1b.c17_pb-one-and-half-b
      | Base Units
      
    .font-source-code
      - const range = Array.from({ length: 8 }, (_, i) => `${i + 1}`)
      each i in ['quarter-', 'half-'].concat(range)
        .flex.mb-half-b
          .w-4b= `${i}b`
          .h-1b.bg-darker(class=`w-${i}b`)

  section.px-1b
    h2.pb-1b.c17_pb-one-and-half-b
      | Column Units
